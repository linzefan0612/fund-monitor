# 基金实时监控助手 - 使用指南

## 📋 目录
1. [环境要求](#环境要求)
2. [安装步骤](#安装步骤)
3. [启动项目](#启动项目)
4. [功能使用](#功能使用)
5. [常见问题](#常见问题)

---

## 环境要求

### 1. 操作系统
- Windows 10 / Windows 11（推荐）
- 也可运行于 macOS / Linux

### 2. Python 环境
需要安装 **Python 3.8 或更高版本**

#### 如何检查是否已安装 Python？
打开命令提示符（按 `Win + R`，输入 `cmd`，回车），输入：
```
python --version
```
- 如果显示 `Python 3.x.x`，说明已安装
- 如果提示"不是内部或外部命令"，说明未安装

#### 如何安装 Python？
1. 访问 Python 官网：https://www.python.org/downloads/
2. 下载 Python 3.10 或更高版本
3. 运行安装程序，**务必勾选** `Add Python to PATH`
4. 点击 `Install Now` 完成安装
5. 重新打开命令提示符，输入 `python --version` 验证安装成功

---

## 安装步骤

### 方式一：一键安装（推荐新手）

1. **双击运行 `start.bat` 文件**
2. 脚本会自动完成以下操作：
   - 检查 Python 环境
   - 安装所需依赖库
   - 启动 Web 服务
3. 看到以下提示表示启动成功：
   ```
   ==================================================
      访问地址: http://localhost:5000
   ==================================================
   ```
4. 打开浏览器，访问 http://localhost:5000

### 方式二：手动安装

#### 第一步：打开命令提示符
按 `Win + R`，输入 `cmd`，回车

#### 第二步：进入项目目录
```
cd /d d:\my-web\fund-monitor
```
> 注意：请将路径替换为实际的项目目录

#### 第三步：安装依赖
```
pip install -r requirements.txt
```
等待安装完成，看到 `Successfully installed...` 表示安装成功

#### 第四步：启动服务
```
python app.py
```

#### 第五步：访问系统
打开浏览器，访问 http://localhost:5000

---

## 启动项目

### 启动成功的标志
控制台显示以下信息：
```
==================================================
基金实时监控助手 启动中...
==================================================
刷新间隔: 30秒
下午提醒时间: 14:40
==================================================
访问地址: http://localhost:5000
==================================================
 * Running on http://0.0.0.0:5000
```

### 如何停止服务？
在命令提示符窗口中按 `Ctrl + C` 即可停止服务

---

## 功能使用

### 1. 添加监控基金

在首页顶部输入区域：

| 输入框 | 说明 | 是否必填 | 示例 |
|--------|------|----------|------|
| 基金代码 | 6位数字基金代码 | **必填** | 000001 |
| 当前持仓金额 | 您目前持有该基金的金额 | 选填 | 10000 |
| 持仓上限金额 | 您计划投资该基金的上限 | 选填 | 50000 |

点击 **「添加基金」** 按钮完成添加

**常用基金代码示例：**
- 000001 - 华夏成长混合
- 110011 - 易方达中小盘混合
- 161725 - 招商中证白酒指数
- 519778 - 交银定期支付双息平衡

### 2. 查看基金数据

添加成功后，基金卡片显示以下信息：

```
┌─────────────────────────────────────┐
│ 华夏成长混合              中风险    │
│ 000001                              │
├─────────────────────────────────────┤
│ 最新净值: 1.127    估算净值: 1.122  │
│ 估算涨跌: -0.48%   最大回撤: 15.2%  │
├─────────────────────────────────────┤
│ 持仓: ¥10,000 / ¥50,000 (20%)       │
│ ████████░░░░░░░░░░░░░░░░░░░░░░░░░░░ │
├─────────────────────────────────────┤
│ 📋 建议: 建议持有观望               │
│ 原因: 当前市场震荡，无明显操作信号  │
└─────────────────────────────────────┘
```

### 3. 编辑持仓

1. 点击基金卡片上的 **「编辑持仓」** 按钮
2. 在弹出窗口中修改：
   - 当前持仓金额
   - 持仓上限金额
3. 点击 **「保存」** 完成修改

### 4. 移除基金

点击基金卡片上的 **「移除」** 按钮，确认后即可移除

### 5. 手动刷新数据

点击顶部的 **「🔄 刷新数据」** 按钮，立即获取最新数据

### 6. 查看下午报告

点击 **「📋 下午报告」** 按钮，查看当日操作建议汇总：

```
📊 下午操作报告

总监控基金: 5 只
建议加仓: 1 只
建议减仓: 1 只
建议低吸: 0 只
建议止盈: 1 只

总建议买入: ¥5,000.00
总建议卖出: ¥3,000.00
```

### 7. 查看风险提醒

当触发监控规则时，页面顶部会显示提醒面板：

| 提醒类型 | 触发条件 | 级别 |
|----------|----------|------|
| 涨跌幅提醒 | 单日涨跌幅 ≥ ±1.5% | ⚠️ 警告 |
| 估值偏差提醒 | 估算与实际净值偏差 ≥ 0.3% | ⚠️ 警告 |
| 持仓股票异动 | 重仓股涨跌幅 > 5% | ⚠️ 警告 |
| 最大回撤提醒 | 最大回撤超过阈值 | 🔴 危险 |

### 8. 同步持仓（图片识别）

点击 **「📥 同步持仓」** 按钮，打开同步持仓弹窗，支持三种方式：

#### 方式一：图片识别（推荐）

支持上传持仓截图，系统自动识别基金信息：

**支持的截图来源：**
- 天天基金 APP 持仓截图
- 支付宝（蚂蚁财富）持仓截图
- 券商 APP 持仓截图

**使用步骤：**
1. 在手机上打开基金 APP，进入持仓页面
2. 截图保存到本地
3. 点击弹窗中的上传区域，或直接拖拽图片到上传区
4. 系统自动进行 OCR 识别
5. 识别完成后，勾选需要添加的基金
6. 可修改持仓金额
7. 点击 **「确认添加选中基金」** 完成导入

**识别内容：**
- 基金名称
- 基金代码
- 持仓金额
- 持有收益率

**注意事项：**
- 确保截图清晰，文字完整
- 如识别不准确，可手动修改金额
- 支持同时上传多张截图

#### 方式二：文本输入

适合手动输入多只基金：

**输入格式：**
```
基金代码,持仓金额,上限金额,持有收益率
```

**示例：**
```
000001,10000,50000,5.5
110022,5000,20000,-2.3
161725,20000,50000,10.2
```

**说明：**
- 每行一只基金
- 基金代码必填（6位数字）
- 其他字段可选，用逗号或空格分隔

#### 方式三：Excel 上传

批量导入大量基金数据：

**使用步骤：**
1. 点击 **「下载 Excel 模板」** 下载模板文件
2. 按模板格式填写基金数据
3. 点击 **「选择 Excel 文件」** 上传
4. 系统自动解析并导入

**Excel 模板格式：**
| 基金代码 | 基金名称 | 当前持仓 | 上限金额 | 持有收益率 |
|----------|----------|----------|----------|------------|
| 000001 | 华夏成长 | 10000 | 50000 | 5.5 |
| 110011 | 易方达中小盘 | 5000 | 20000 | -2.3 |

---

## 智能建议说明

系统会根据以下策略自动生成操作建议：

| 建议类型 | 触发条件 | 建议 |
|----------|----------|------|
| 🟢 建议加仓 | 估值低位 + 趋势向上 + 回撤较小 | 可考虑增加投资 |
| 🔴 建议减仓 | 估值高位 + 连续上涨 + 回撤扩大 | 可考虑减少仓位 |
| 🟡 建议持有观望 | 中期震荡 + 无明确方向 | 暂不操作，继续观察 |
| 🔵 建议分批低吸 | 单日大跌 ≥ 3% | 可考虑分批买入 |
| 🟠 建议止盈部分仓位 | 单日大涨 ≥ 4% | 可考虑卖出部分获利 |

---

## 常见问题

### Q1: 启动时提示"pip不是内部或外部命令"
**原因**：Python 未正确安装或未添加到系统环境变量

**解决方案**：
1. 重新安装 Python，勾选 `Add Python to PATH`
2. 或手动添加 Python 到环境变量

### Q2: 安装依赖时网络超时
**原因**：网络连接问题或 pip 源访问慢

**解决方案**：使用国内镜像源
```
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

### Q3: 启动后页面无法访问
**原因**：端口被占用或防火墙阻止

**解决方案**：
1. 检查 5000 端口是否被占用
2. 尝试关闭防火墙或添加例外规则
3. 修改 `app.py` 最后一行的端口：
   ```python
   app.run(host='0.0.0.0', port=5001, debug=True)  # 改为其他端口
   ```

### Q4: 添加基金时提示"添加失败"
**原因**：基金代码错误或网络问题

**解决方案**：
1. 确认基金代码为 6 位数字
2. 检查网络连接是否正常
3. 确认能访问天天基金网

### Q5: 数据不更新
**原因**：自动刷新功能未启动或网络问题

**解决方案**：
1. 点击手动刷新按钮
2. 检查控制台是否有错误信息
3. 刷新浏览器页面（F5）

### Q6: 如何配置代理？
如果需要通过代理访问网络，编辑 `config.py` 文件：

```python
# 找到以下代码，取消注释并修改
PROXY = {
    'http': 'http://127.0.0.1:7890',
    'https': 'http://127.0.0.1:7890'
}
```

### Q7: 如何修改刷新间隔？
编辑 `config.py` 文件：
```python
# 数据刷新间隔（秒）
REFRESH_INTERVAL = 30  # 改为你想要的秒数
```

### Q8: 如何修改提醒阈值？
编辑 `config.py` 文件，找到对应参数修改：
```python
# 单日涨跌幅提醒阈值（%）
PRICE_CHANGE_THRESHOLD = 1.5  # 改为你想要的值

# 最大回撤风险提醒阈值（%）
MAX_DRAWDOWN_THRESHOLD = 15.0
```

---

## 技术支持

如有问题，请检查：
1. Python 版本是否 ≥ 3.8
2. 依赖是否正确安装
3. 网络连接是否正常
4. 控制台错误信息

---

## ⚠️ 免责声明

1. 本工具仅供学习研究使用，不构成任何投资建议
2. 数据来源于公开接口，请合理使用
3. 投资有风险，决策需谨慎
4. 使用本工具产生的任何损失，开发者不承担责任
